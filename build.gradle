apply plugin: 'groovy'
// apply plugin: 'codenarc'
apply plugin: 'idea'
apply from: 'idea.gradle'
apply plugin: 'cobertura'


// --- properties ---
ext.ideaInstallationPath = 'C:/Program Files (x86)/JetBrains/IntelliJ IDEA Community Edition 12.0'
ext.ideaJdk = 'IDEA IC-117.418'
ext.sourceCompatibility = 1.6
// --- properties ---

// workaround for http://issues.gradle.org/browse/GRADLE-2538
sourceSets.main.java.srcDirs = []
sourceSets.main.groovy.srcDir 'src/main/java'


task wrapper(type: Wrapper) {
    gradleVersion = '1.4'
}

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.mapvine:gradle-cobertura-plugin:1.0'
    }
}


cobertura {
    format = 'html'
    includes = ['**/*.java', '**/*.groovy']
}

repositories {
    mavenCentral()
	mavenLocal()
}

dependencies {
//    compile 'org.pitest:pitest:0.28'
	compile fileTree(dir: ideaInstallationPath + '/lib', include: '*.jar')
    testCompile 'org.spockframework:spock-core:0.7-groovy-1.8'
    testCompile 'org.objenesis:objenesis:1.2'
    groovy group: 'org.codehaus.groovy', name: 'groovy-all', version: '1.8.8'
}

metaInf << files('/META-INF/plugin.xml')
